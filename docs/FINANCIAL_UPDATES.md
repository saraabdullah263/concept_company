# تحديثات النظام المالي 💰

## التغييرات المنفذة

### 1️⃣ إضافة حالة للمصاريف
- ✅ إضافة عمود `status` لجدول `expenses`
- ✅ القيم المتاحة: `active` (نشط), `cancelled` (ملغي)
- ✅ القيمة الافتراضية: `active`
- ✅ استبعاد المصاريف الملغاة من الحسابات

### 2️⃣ دمج تكاليف المحارق في المصاريف
- ✅ إضافة تكاليف المحارق (`incinerator_deliveries.total_cost`) للمصاريف الإجمالية
- ✅ عرض تكاليف المحارق بشكل منفصل في التقارير
- ✅ حساب إجمالي المصاريف = المصروفات العادية + تكاليف المحارق

### 3️⃣ تحديث حالات الفواتير
- ✅ حذف حالة `draft` (مسودة)
- ✅ حذف حالة `sent` (مرسلة)
- ✅ الحالات المتبقية: `paid` (مدفوعة), `overdue` (متأخرة), `cancelled` (ملغاة)

### 4️⃣ تحديث Dashboard
- ✅ حساب المصاريف يستبعد الملغاة
- ✅ إضافة تكاليف المحارق للمصاريف الإجمالية
- ✅ عرض منفصل لمستحقات المحارق

### 5️⃣ تحديث التقارير المالية
- ✅ عرض المصروفات العادية بشكل منفصل
- ✅ عرض تكاليف المحارق بشكل منفصل
- ✅ حساب إجمالي المصاريف الشامل
- ✅ تصدير Excel يتضمن sheet منفصل لتكاليف المحارق
- ✅ 5 بطاقات ملخص: الإيرادات، المصروفات العادية، تكاليف المحارق، إجمالي المصاريف، صافي الربح

### 6️⃣ تحديث صفحة المصاريف
- ✅ إضافة حقل الحالة في نموذج المصروف
- ✅ إضافة فلتر لعرض/إخفاء المصاريف الملغاة
- ✅ حساب الإجمالي يستبعد المصاريف الملغاة

## الملفات المعدلة

### ملفات SQL
1. `add_expense_status.sql` - إضافة حالة للمصاريف
2. `update_invoice_status.sql` - تحديث حالات الفواتير

### ملفات JavaScript/React
1. `src/pages/Dashboard.jsx` - تحديث حساب المصاريف
2. `src/pages/reports/Reports.jsx` - تحديث التقرير المالي
3. `src/components/accounting/InvoiceForm.jsx` - تحديث حالات الفواتير
4. `src/components/accounting/ExpenseForm.jsx` - إضافة حقل الحالة
5. `src/pages/accounting/Expenses.jsx` - إضافة فلتر المصاريف الملغاة

## خطوات التطبيق

### 1. تشغيل ملفات SQL في Supabase
```sql
-- 1. إضافة حالة للمصاريف
-- قم بتشغيل محتوى ملف: add_expense_status.sql

-- 2. تحديث حالات الفواتير
-- قم بتشغيل محتوى ملف: update_invoice_status.sql
```

### 2. إعادة تشغيل التطبيق
```bash
npm run dev
```

## النتائج المتوقعة

### في Dashboard:
- إجمالي المصاريف = المصروفات العادية + تكاليف المحارق
- استبعاد المصاريف الملغاة من الحسابات

### في التقارير المالية:
- 5 بطاقات ملخص تعرض التفاصيل الكاملة
- جدول منفصل للمصروفات العادية
- جدول منفصل لتكاليف المحارق
- ملف Excel يحتوي على 4 sheets:
  1. الفواتير
  2. المصروفات العادية
  3. تكاليف المحارق
  4. الملخص

### في صفحة المصاريف:
- إمكانية تحديد حالة المصروف (نشط/ملغي)
- فلتر لعرض/إخفاء المصاريف الملغاة
- الإجمالي يحسب المصاريف النشطة فقط

### في نموذج الفاتورة:
- 3 حالات فقط: مدفوعة، متأخرة، ملغاة
- القيمة الافتراضية: مدفوعة

## ملاحظات مهمة

⚠️ **يجب تشغيل ملفات SQL قبل استخدام التطبيق**

✅ **جميع المصاريف القديمة ستكون بحالة "نشط" تلقائياً**

✅ **الفواتير القديمة بحالة draft أو sent ستتحول إلى "مدفوعة"**

✅ **تكاليف المحارق تُحسب الآن ضمن المصاريف الإجمالية**

## الدعم

في حالة وجود أي مشاكل:
1. تأكد من تشغيل ملفات SQL
2. تحقق من console المتصفح للأخطاء
3. تأكد من وجود بيانات في جدول `incinerator_deliveries`
